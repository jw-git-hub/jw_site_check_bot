"""Все пороги оценок — в одном месте (ТЗ, 6.6). Стартовые значения уточняются на живом прогоне (задача 23)."""

LCP_GOOD_MS = 2500           # Core Web Vitals: до 2,5 с — хорошо
LCP_FIX_MS = 4000            # до 4 с — стоит поправить, дольше — плохо
TBT_ALLOWANCE_MS = 200       # скриптам потерянное время считается сверх этого
SERVER_ALLOWANCE_MS = 600    # серверу — сверх этого
CAUSE_MIN_MS = 500           # причину медленности называем, если у лидера не меньше
PAGE_GOOD_BYTES = 2_621_440  # 2,5 МБ
PAGE_FIX_BYTES = 5_242_880   # 5 МБ
COMPRESS_MIN_RATIO = 2       # «ужать в N раз» пишем от двух раз
CERT_WARN_DAYS = 14
SHORT_CERT_DAYS = 30         # короткие сертификаты — предупреждать в последнюю четверть срока
SHORT_CERT_WARN_SHARE = 0.25
